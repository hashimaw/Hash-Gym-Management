import 'package:flutter/material.dart';

class ViewMembersDesktop extends StatelessWidget {
  const ViewMembersDesktop({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('view members');
  }
}
